#include <bits/stdc++.h>

using namespace std;
int main()
{
    int n;
    cin >> n;
    vector<int > v;
    for(int i=0;i<n;i++)
    {
        int s;
        cin >> s;
        v.push_back(s);
    }
    int sum=accumulate(v.begin(),v.end(),0);
    cout << sum << "\n";







    return 0;
}
