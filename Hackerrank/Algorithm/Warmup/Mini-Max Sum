#include <bits/stdc++.h>

typedef long long ll;
using namespace std;
#define all(c) (c).begin(),(c).end()

int main() {
    vector<int> arr(5);
    for(int i = 0; i < 5; i++){
       cin >> arr[i];
    }
    vector <int > :: iterator it1,it2;

   ll yes = *max_element(all(arr));
   ll no =*min_element(all(arr));

    ll sum=0;
    for(int i = 0; i < 5; i++)
        sum+=arr[i];


    cout << sum -yes << " " << sum-no << "\n";
    return 0;
}
